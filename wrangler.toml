name = "edgetunnel1"
main = "src/worker-vless.js"
compatibility_date = "2024-04-15"

[vars]
# هذا هو الـ UUID الذي كان موجوداً في كودك الأصلي
UUID = "example_dev_token"

# ملاحظة: إذا كنت تستخدم هذا العامل (Worker) لتطبيقات مثل V2Ray أو السيرفرات الخاصة، 
# يفضل لاحقاً استبدال "example_dev_token" بـ UUID حقيقي لزيادة الأمان.
